# ContractPilot 환경 설정
# 이 파일을 .env로 복사한 후 값을 설정하세요

# ===========================================
# 앱 설정
# ===========================================
APP_ENV=development
DEBUG=true
CORS_ORIGINS=http://localhost:3000,https://contractpilot.pages.dev

# ===========================================
# LLM 제공자 설정
# ===========================================
# 사용할 LLM 제공자 선택: openai | upstage | anthropic | local
# 한국 개인정보보호법 준수를 위해 upstage 또는 local 권장
LLM_PROVIDER=openai

# OpenAI 설정 (LLM_PROVIDER=openai 일 때)
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o

# Upstage Solar 설정 (LLM_PROVIDER=upstage 일 때)
# 한국 서버에서 운영되어 개인정보보호법 준수에 유리
UPSTAGE_API_KEY=your-upstage-api-key
UPSTAGE_MODEL=solar-pro
UPSTAGE_BASE_URL=https://api.upstage.ai/v1/solar

# Anthropic Claude 설정 (LLM_PROVIDER=anthropic 일 때)
ANTHROPIC_API_KEY=your-anthropic-api-key
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# 로컬 LLM 설정 (LLM_PROVIDER=local 일 때)
# Ollama, vLLM 등 로컬 서버 사용
LOCAL_LLM_BASE_URL=http://localhost:11434/v1
LOCAL_LLM_MODEL=llama3.1:8b

# ===========================================
# 개인정보 보호 설정
# ===========================================
# 개인정보 익명화 활성화 (true 권장)
ANONYMIZE_PERSONAL_DATA=true
# 금액 정보 보존 (계약서 분석 시 true 권장)
PRESERVE_AMOUNTS_IN_ANONYMIZATION=true

# ===========================================
# 임베딩 모델 설정
# ===========================================
EMBEDDING_MODEL=text-embedding-3-large

# ===========================================
# 벡터 DB 설정 (선택사항)
# ===========================================
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_INDEX_NAME=contract-pilot

# ===========================================
# 데이터베이스 설정 (선택사항)
# ===========================================
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/contractpilot
